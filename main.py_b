import asyncio
import logging
from aiogram import Bot, Dispatcher, Router
from handlers import ask_gpt



async def main():
    bot = Bot(token='6067532093:AAHckhCojDtjosklmirPqQf85JuIfnohUeI')
    router = Router()
    dp = Dispatcher()
    dp.include_routers(router, ask_gpt.router)
    await dp.start_polling(bot)


if __name__ == "__main__":
    logging.basicConfig(level=logging.DEBUG)
    asyncio.run(main())
